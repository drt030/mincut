# UX Flow 2 — Cost vs Maturity mode comparison

HEAD: `(post 96cbb6e)`
Run at: 2026-05-11 01:16 EDT

This flow's substance overlaps significantly with Flow 4 (color-mode consistency), already run with full scoring at HEAD `113a087`. Below is a slimmer pass that emphasizes the *comparative* read between cost mode and maturity mode for the same set of children.

## Findings

For the parcel-sorting flagship at HEAD `(post 96cbb6e)`:

| Child | Cost (typ) | Maturity label | Cost-mode tint | Maturity-mode tint |
| --- | --- | --- | --- | --- |
| industrial_robot_arm_body | 60,000 RMB | commercially_available | warm (red-orange) | green |
| conveyor_integration | 55,000 RMB | early_deployment | warm | lime |
| parcel_manipulation_or_diverter | 4,000 RMB direct (89.7k effective) | prototype | mid (effective warmth via max-of-children) | amber |
| maintenance_workflow | 5,000 RMB | prototype | cool | amber |
| safety_system | 12,000 RMB | commercially_available | cool | green |

Distinct edge stroke values per mode (Flow 4 data): cost 9 / maturity 3 / overall 11 / bottleneck 12. **Cost gives the richest cross-graph variance because the data is more numeric; maturity hits Likert buckets and reads as fewer colors.** Worth telling new users this when they wonder why maturity mode looks "less colorful".

## Summary

- This Flow 2 supplements Flow 4 with a cross-mode comparison example. No assertions; pure illustration for the morning hand-off.
- Pass/fail: **pass** (no regressions surfaced)
