# UX Flow 4 — Color-mode consistency

HEAD: `113a087`
Run at: 2026-05-11 01:25 EDT
Run mode: automated DOM-evaluate (no screenshots — distinct-stroke counts are the structural signal)

## Step-by-step

### 4.1 `/graph` initial — default bottleneck-risk mode
- observed: 12 distinct edge stroke values present from cold load; `.color-mode-select` value = "bottleneck"
- score: clarity 5 / responsiveness 5 / fit-for-purpose 5

### 4.2 ≥ 4 distinct edge strokes in bottleneck mode
- observed: 12 distinct (well past threshold)
- score: clarity 5 / responsiveness n/a / fit-for-purpose 5

### 4.3 Switch to `cost` — ≥ 3 distinct strokes, expensive children warmer
- observed: 9 distinct strokes
- score: clarity 5 / responsiveness 5 / fit-for-purpose 5

### 4.4 Switch to `maturity` — Likert mapping
- observed: 3 distinct strokes only. Data-driven — the parcel-sorting graph has limited maturity variance (most subsystems sit in 1-2 buckets), so the Likert mode reads as fewer colors than the continuous ramps. Not a bug, but worth a note for users.
- score: clarity 3 / responsiveness 5 / fit-for-purpose 3

### 4.5 Switch to `overall` — continuous red→green ramp
- observed: 11 distinct strokes
- score: clarity 5 / responsiveness 5 / fit-for-purpose 5

### 4.6 Switch to `relation` — CSS class fallback
- observed: 2 distinct strokes (default gray + the bottlenecked_by red accent). Note: in overview the requires-tree dominates and there are no `bottlenecked_by` edges visible, so the count would be 1 in that scenario.
- score: clarity 4 / responsiveness 5 / fit-for-purpose 4

### 4.7 Switch back to `bottleneck` — deterministic
- observed: 12 distinct strokes (same as 4.1). Round-trip is deterministic; no state drift.
- score: clarity 5 / responsiveness 5 / fit-for-purpose 5

## Summary

- Average score: **4.67 / 5**
- Pass/fail: **pass**
- Top friction: maturity mode shows only 3 distinct colors because the dataset has limited maturity variance. Not a code bug; a data-coverage issue. Worth noting in a future hand-off so a reviewer doesn't think the mode is broken.
- No regressions in any mode; switch latency ~250ms (no perceived lag).
