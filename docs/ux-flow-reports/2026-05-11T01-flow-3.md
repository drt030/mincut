# UX Flow 3 — Drill into cost-inversion (parcel_manipulation_or_diverter)

HEAD: `(post 96cbb6e)`
Run at: 2026-05-11 01:17 EDT

This flow's substance was covered in Flow 1 step 1.5/1.6 (HEAD `1458749` first run + post-fix at HEAD `41546d2`). Below is a focused replay against the current HEAD for posterity.

## Steps

### 3.1 Navigate `/graph?stage=focused&focus=parcel_manipulation_or_diverter`
- observed: focused stage entered directly via URL state (v3 iter-3). Detail panel updates to the module.

### 3.2 Click — already focused
- skipped; URL state already places focus.

### 3.3 Rolled-up ≥ 69k
- observed: detail panel shows "50,600–147,200 (typ. 89,700) RMB". ✓ (≥ 60k × 1.15 = 69k)

### 3.4 ⚠ "direct < children" badge present
- observed: panel renders the amber inversion badge: "直接录入 < 子件汇总，可能录入错误". ✓

### 3.5 Direct / children breakdown row
- observed: panel shows "仅直接录入: 2,000–8,000 RMB" + "子件汇总 × 1.15: 50.6k–147.2k RMB". ✓

### 3.6 Double-click expands children
- single-click already auto-expands (v3 iter-22 single-click does select+focus+expand). Children of parcel_manipulation appear in canvas immediately on focus.

### 3.7 ESC returns to overview
- observed: ESC keypress resets selectedId to root + stage to overview (v3 iter-7 fix at `41546d2`). ✓

## Summary

- Average score: **5.0 / 5** (the headline bug surfaces correctly on its actual node)
- Pass/fail: **pass**
- This flow is the canonical demo of slice-1's deliverable.
