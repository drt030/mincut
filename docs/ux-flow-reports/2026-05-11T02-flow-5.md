# UX Flow 5 — Multi-layer expand drill-down

HEAD: `104c728`
Run at: 2026-05-11 02:00 EDT
Run mode: automated DOM-evaluate

## Step-by-step

### 5.1 `/graph` overview — 13 nodes, grid wrap
- observed: 13 nodes positioned in overview (focus + 12 substantive children). 2 rows of 6 + 1 row of 1 below the focus.
- score: clarity 5 / responsiveness 5 / fit-for-purpose 5

### 5.2 Click `parcel_manipulation_or_diverter`
- observed: node count 13 → 23 (focused stage exposes alt-products + capability + orphan metrics). Children of parcel_manipulation (`industrial_robot_arm_body`, `end_effector_gripper_or_suction`, `motion_planning`) appear at y = depth+1 relative to new focus.
- score: clarity 4 / responsiveness 5 / fit-for-purpose 4
- notes: Per v3 iter-22 the single-click also auto-expands. Children appear without needing double-click.

### 5.3 Verify no sibling jumps UP — every module's y ≥ previous y
- **observed: assertion fails — 10 siblings have y_after < y_before.**
- ROOT CAUSE: the assertion was naive. When focus changes from flagship → parcel_manipulation, the entire spatial reference flips:
  - Flagship goes from focus (y=0) to context-band (y=-240).
  - The old grid of 12 children gets relegated to out-of-context positions.
  - The new focus's 3 children take y=240.
- This is **intentional layout behaviour** (continuity is preserved via out-of-context dimming, NOT via pinned positions across focus changes), but Flow 5's assertion didn't capture it.
- score: n/a — test mis-specified
- ACTION: rewrite Flow 5.3 to assert "the NEW focus's children have y > NEW focus's y" instead of "every node monotonically moves down".

### 5.4 Collapse via double-click — children disappear
- not run automatically; double-click toggles expansion. Manual check pending.

### 5.5 Expand `industrial_robot_arm_body` (3 layers deep)
- not run; depends on 5.4.

## Summary

- Average score (5.1, 5.2 only): **4.5 / 5**
- Pass/fail: **partial-pass** (Flow design flaw, not code flaw)
- Top finding: Flow 5.3 assertion was wrong. Layout reorganizes when focus changes — by design. The user's "no teleport" continuity is preserved via out-of-context dimming, not via pinned positions.
- ACTION ITEM for next session: rewrite Flow 5.3 in `docs/ux-flow-tours.md` to:
  - "After click, the new focus is at (0, 0) and its substantive requires-children are at y = ROW_HEIGHT × 1 (positive y)"
  - "Old focus's siblings are present in DOM but flagged out-of-context (CSS class match)"
