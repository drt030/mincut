# UX Flow 1 — New user finds a bottleneck

HEAD: `1458749` (after iter-fixes)
Run at: 2026-05-10 22:00 EDT

## Step-by-step

### 1.1 navigate `/` — home renders hero
- screenshot: `.tmp/civ-ralph/flow-1-run/01-home.png`
- observed: hero "Capability Graph Explorer", current product callout "30 万 RMB 包裹分拣机器人", visual signals legend
- score: clarity 4 / responsiveness 5 / fit-for-purpose 4
- notes: dense text but the "Open the active graph" CTA is clearly placed

### 1.2 navigate `/graph` — overview stage with 13 compact nodes
- screenshot: `.tmp/civ-ralph/flow-1-run/02a-graph-no-fit.png` (broken initial), `02b-graph-fit.png` (after manual fit)
- observed: 13 nodes positioned (1 focus + 12 children), 13 distinct heat colors. **Initial viewport is (0,0,1) — user sees blank canvas until they click Fit View.** Bug.
- score: clarity 5 / responsiveness 2 / fit-for-purpose 2
- notes: **initial fitView misfire** is a real regression — flagged for a follow-up iter. After manual fit, layout is clean.

### 1.3 find warmest child via heat color
- observed (DOM evaluate): warmest = `parcel_manipulation_or_diverter` color `#eda00e` (amber). This is exactly the cost-inversion node — algorithm correctly surfaced it as highest risk.
- score: clarity 5 / responsiveness 5 / fit-for-purpose 5
- notes: bottleneck-risk algorithm working as intended end-to-end. The user's "一眼看到瓶颈" wish met.

### 1.4 click warmest child — stage transitions to focused
- screenshot: `.tmp/civ-ralph/flow-1-run/04-focused.png`
- observed: scale=0.8, selectedCount=1, compactCount=0 (cards now full-mode), "↩ 回到全局" button appears
- score: clarity 5 / responsiveness 5 / fit-for-purpose 4
- notes: state machine fires cleanly. Animation smooth.

### 1.5 + 1.6 detail panel: cost rollup + ⚠ inversion badge
- screenshot: `.tmp/civ-ralph/flow-1-run/05-inversion-badge.png` (post-fix)
- observed (pre-fix at HEAD 3d1e871): **detail panel had no cost rollup card for module-kind nodes**. The ⚠ badge never surfaced on the actual bug node. **Real regression**.
- post-fix observed: panel now shows
  - 汇总成本 50,600–147,200 (typ. **89,700**) RMB
  - 仅直接录入: 2,000–8,000 RMB
  - 子件汇总 × 1.15: 50.6k–147.2k RMB
  - ⚠ 直接录入 < 子件汇总，可能录入错误 (amber badge)
- score (post-fix): clarity 5 / responsiveness 5 / fit-for-purpose 5
- notes: **fix committed at `3cfe9bd`**. The inversion the user originally reported is now exposed on the very node where it lives.

### 1.7 ESC — return to overview
- observed (pre-fix at HEAD 3cfe9bd): ESC flipped stage but kept selectedId on the clicked module. "回到全局" showed the previous module's 3-child subtree, not the flagship overview.
- post-fix observed: ESC sets both `stage=overview` AND `selectedId=rootNodeId`. After ESC the user reliably returns to the flagship's 13-node overview regardless of drill depth.
- score (post-fix): clarity 5 / responsiveness 5 / fit-for-purpose 5
- notes: **fix committed at `41546d2`**. The toolbar "↩ 回到全局" button gets the same fix.

## Summary

- Average score (post-fix): **4.6 / 5**
- Pass/fail: **pass**
- Top friction (still unresolved): step 1.2 initial viewport is (0,0,1) — user lands on /graph and sees a blank canvas until they manually click Fit View. Flagged for a follow-up iter.
- Bugs caught by this tour: **2**
  - Module/equipment/material detail panel didn't render cost rollup → fixed `3cfe9bd`
  - ESC didn't reset selectedId → fixed `41546d2`
- Action item: next iter — fix initial fitView so the canvas is visible on first paint without user intervention.
